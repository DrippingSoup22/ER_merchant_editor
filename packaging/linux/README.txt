ER Merchant Editor for Linux

Run ./ERMerchantEditor from this directory. The GUI uses the desktop's
Wayland/X11 and graphics libraries. The current file-dialog adapter also
requires one of zenity, matedialog, or qarma to be installed.

The included .desktop file is packaging metadata for desktop integrators; a
future portal-based dialog adapter will remove the external dialog command.
